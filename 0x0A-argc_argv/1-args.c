#include<stdio.h>

/**
 * main - Entry point
 * @argc: argc parameter
 * @argv: array of command
 * Return: 0(Success)
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d\n", argc - 1);
return (0);
}
