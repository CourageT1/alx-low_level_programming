#include "main.h"

/**
 * get_endianness - checks machine
 *
 * Return: 0 for big, 1 for little
 */
int get_endianness(void)
{
int i = 1;
unsigned int i = 1;
{
if (*c)
return (1);
}
else
return (0);
}
